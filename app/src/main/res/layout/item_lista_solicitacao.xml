<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:background="@color/colorPrimaryDark"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/icon_solicitacao"
        android:src="@drawable/icone_add_solicitacao"
        android:layout_marginTop="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginLeft="20dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <br.com.wasys.gn.motorista.widgets.CustomTextView
        android:id="@+id/lbl_titulo"
        android:layout_width="fill_parent"
        android:text="Preciso solicitar revisão no valor do transporte"
        android:layout_toRightOf="@+id/icon_solicitacao"
        android:layout_marginTop="30dp"
        android:layout_height="wrap_content"
        app:custom_typeface="SourceSansPro-Regular.otf"
        android:textColor="@color/white"
        android:textSize="22sp"
        android:layout_weight="1.0"/>

</RelativeLayout>